# 2026-09-14 오늘 정리 중단 후속 수정

## 원인과 범위

- 운영 `summarizeSnackChatUnread`의 수정 시각은 `2026-09-04T16:45:32.448Z`, 소스 해시는 `f96c06d565fdc9e16a808f3623c5e40ad82ef639`였음.
- 9월 14일 13:31 UTC 호출 로그는 HTTP 200/cache hit. 현재 앱은 `paged: true` 응답을 요구하지만 이전 서버 구현에는 페이지 응답이 없어 클라이언트가 결과 표시 전에 예외 처리하고 `정리 중단 · 재개 가능 · 0`을 표시하는 경로를 확인.
- 이전 후속 배포에는 검색/멘션 callable 4개만 포함됐으며 정리 함수는 포함되지 않았음.

## 수정

- 기존 페이지 단위 정리 구현을 동일한 callable에 한정해 배포. `paged` 없는 구버전 요청 경로 유지. 채팅 송수신/읽음/알림 Functions, Rules, 인덱스는 배포 대상이 아님.
- 서버: 초기 요청의 기기 시계가 서버보다 최대 5분 빠른 경우 서버 시각으로 제한. 후속 커서는 확정된 snapshot을 유지하며 만료/권한 검증은 유지. 정리 로그 소요 시간은 오래된 snapshot이 아닌 실제 요청 시작 시각 기준.
- 앱: 서버 형식 불일치/인증/권한/요청 한도/일시 오류를 구분하는 한·영·중 안내. 오류에 의미 없는 `0` 숫자를 붙이지 않음.
- 처리 대기 응답에서도 서버 snapshot/sequence를 반영·보관하여 대기 재요청의 범위를 고정. 재개 시 문맥 제한 상태 초기화, 네트워크 시간 초과 구분.
- 최초 응답 유실에 대비한 기존 요청 snapshot 보관, 계정별 요청 분리, 원문 재검증, 중복 start 병합 정책 유지. 사용자 메시지·읽음·설정 데이터 삭제/초기화 없음.

## 검증 및 배포

- 요청에 따라 앱 빌드/테스트 실행 생략. Dart formatter 구문 처리, `git diff --check` 확인. 회귀 테스트 코드는 추가했으나 실행하지 않음.
- Firebase 배포에 필수인 TypeScript 컴파일 통과.
- 배포 대상: `flutterproject3-af322`, `us-central1`, `functions:summarizeSnackChatUnread`만.
- 배포 성공 후 운영 API에서 `ACTIVE` 확인. 수정 시각 `2026-09-14T13:38:52.318Z`, 소스 해시 `42348515da1e8424fc06e190b7e808927c02e192`.
- 실기기 로그인 상태에서 AI 생성 완료/결과 표시, 장시간 여러 페이지 처리, 모든 구버전 앱의 실제 동작은 미확인. 운영 사용자 토큰을 만들어 가장하거나 테스트 대화를 쓰지 않음.
- 서버 형식 수정은 기존 페이지 정리 앱에도 적용. 상세 오류 안내 등 앱 코드 변경은 새 앱 배포가 필요.

## 복귀

필요 시 다음 앱 릴리스에서 기존 `SNACK_CHAT_PAGED_SUMMARY=false`로 레거시 정리 경로를 사용. 구버전 서버로 먼저 되돌리면 현재 paged 클라이언트가 다시 중단되므로, 앱 호환 대책 없이 함수만 이전 버전으로 복구하지 말 것. 사용자 데이터/읽음/알림 설정은 복귀에서도 초기화하지 않음.
